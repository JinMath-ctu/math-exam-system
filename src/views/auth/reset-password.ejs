<%- include('../partials/header', { bodyClass: 'is-auth' }) %>
<main id="main-content" class="page-auth">
  <div class="auth-decor" aria-hidden="true">
    <span class="d1">π</span>
    <span class="d2">∑</span>
    <span class="d3">√</span>
    <span class="d4">Δ</span>
  </div>

  <div class="auth-shell">
    <div class="auth-card">
      <div class="auth-card-logo">
        <a href="/" aria-label="JinMath - Trang chủ">
          <img src="/images/jinmath-logo.png" alt="JinMath" width="180" height="52">
        </a>
      </div>

      <h1>Đặt lại mật khẩu</h1>
      <p class="auth-subtitle">Nhập mã 6 số trong email và mật khẩu mới.</p>

      <%- include('../partials/flash') %>

      <form action="/auth/reset-password" method="POST" class="form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="form-group">
          <label for="email">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            value="<%= (formData && formData.email) ? formData.email : '' %>"
            required
            autocomplete="email"
          >
        </div>

        <div class="form-group">
          <label for="code">Mã xác nhận</label>
          <input
            type="text"
            id="code"
            name="code"
            value="<%= (formData && formData.code) ? formData.code : '' %>"
            required
            inputmode="numeric"
            autocomplete="one-time-code"
            pattern="[0-9]{6}"
            maxlength="6"
            placeholder="Nhập 6 chữ số"
          >
        </div>

        <div class="form-group">
          <label for="password">Mật khẩu mới</label>
          <div class="password-field">
            <input
              type="password"
              id="password"
              name="password"
              required
              minlength="6"
              autocomplete="new-password"
              placeholder="Ít nhất 6 ký tự"
            >
            <button type="button" class="password-toggle" data-password-toggle="password" aria-label="Hiện mật khẩu" aria-pressed="false">
              <svg class="icon-eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                <circle cx="12" cy="12" r="3"></circle>
              </svg>
              <svg class="icon-eye-off" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-8-11-8a21.77 21.77 0 0 1 5.06-5.94"></path>
                <path d="M9.9 4.24A10.94 10.94 0 0 1 12 4c7 0 11 8 11 8a21.82 21.82 0 0 1-2.16 3.19"></path>
                <path d="M14.12 14.12a3 3 0 1 1-4.24-4.24"></path>
                <line x1="1" y1="1" x2="23" y2="23"></line>
              </svg>
            </button>
          </div>
        </div>

        <div class="form-group">
          <label for="passwordConfirm">Xác nhận mật khẩu</label>
          <input
            type="password"
            id="passwordConfirm"
            name="passwordConfirm"
            required
            minlength="6"
            autocomplete="new-password"
            placeholder="Nhập lại mật khẩu"
          >
        </div>

        <button type="submit" class="btn btn-primary btn-block">Lưu mật khẩu mới</button>
      </form>

      <p class="auth-footer">
        <a href="/auth/login">Quay lại đăng nhập</a>
      </p>
    </div>
  </div>
</main>
<%- include('../partials/footer') %>
